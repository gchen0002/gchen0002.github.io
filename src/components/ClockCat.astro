<div class="clock-cat" data-clock-cat hidden>
  <span class="cat-note" data-cat-note hidden>hi, recruiter 👋</span>
  <button class="cat-button" type="button" aria-label="Say hello to the little cat">
    <svg viewBox="0 0 100 80" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
      <g class="cat-awake">
        <path class="cat-fill" d="M27 77C24 59 33 49 50 49S76 61 73 77" />
        <g class="cat-head">
          <path class="cat-fill" d="M24 43 22 16 39 27Q50 23 61 27L78 16 76 43C82 64 65 69 50 69S18 64 24 43Z" />
          <path class="cat-ear" d="m27 26 8 5m30 0 8-5" />
          <path d="M36 45v3m28-3v3m-17 4 3 2 3-2m-3 2v4m0 0q-4 4-7 0m7 0q4 4 7 0M28 51l-12-3m12 8-12 2m56-7 12-3m-12 8 12 2" />
        </g>
        <path class="cat-fill" d="M28 77v-6q5-7 10 0v6m24 0v-6q5-7 10 0v6" />
      </g>
      <g class="cat-asleep">
        <path class="cat-fill" d="M19 73C12 51 38 36 62 45 85 51 89 72 74 76H32C14 76 14 62 29 63q14 0 14 10" />
        <path class="cat-fill" d="m54 58 1-16 11 8 12-5-2 18q-2 13-17 8-9-3-5-13Z" />
        <path d="m60 60 4 2 3-3m4 6-3 1m-13 2-8-1" />
        <path class="cat-zzz" d="M26 33h8l-8 8h8m6-21h10L40 30h10" />
      </g>
    </svg>
  </button>
</div>

<style>
  .clock-cat { position: absolute; right: 6px; bottom: calc(100% - 8px); width: 82px; height: 66px; z-index: 3; color: #e0d7e9; }
  .clock-cat[hidden], .cat-note[hidden] { display: none; }
  .cat-button { width: 100%; height: 100%; padding: 0; border: 0; background: none; border-radius: 14px; }
  .cat-button svg { width: 100%; height: 100%; overflow: visible; }
  .cat-fill { fill: #17151a; }
  .cat-ear, .cat-zzz { stroke: #a99bb9; }
  .cat-head { transform-box: fill-box; transform-origin: center 80%; transition: transform 220ms ease; }
  .cat-button:hover .cat-head, .cat-button:focus-visible .cat-head { transform: rotate(-7deg); }
  .cat-asleep { display: none; }
  .clock-cat[data-pose="asleep"] .cat-awake { display: none; }
  .clock-cat[data-pose="asleep"] .cat-asleep { display: block; }
  .cat-note { position: absolute; right: calc(100% + 6px); top: 17px; padding: 8px 11px; border: 1px solid #d2c3ad; border-radius: 11px 11px 3px 11px; background: #fff3d9; color: #29242e; white-space: nowrap; font: 12px/1.4 'DM Sans Variable', sans-serif; box-shadow: 0 4px 15px #0002; }
  @media (prefers-reduced-motion: no-preference) {
    .clock-cat[data-entering] .cat-button { animation: cat-peek 1200ms cubic-bezier(.2,.7,.2,1) both; }
  }
  @keyframes cat-peek { from { opacity: 0; transform: translateY(26px) scale(.88); } 35% { opacity: 1; } to { opacity: 1; transform: translateY(0) scale(1); } }
  @media (max-width: 760px) {
    .clock-cat { left: calc(100% + 12px); right: auto; top: -29px; bottom: auto; width: 64px; height: 54px; }
    .cat-note { left: calc(100% + 3px); right: auto; top: 17px; width: 88px; white-space: normal; font-size: 11px; padding: 6px 9px; }
  }
  @media (max-width: 360px) { .cat-note { left: auto; right: -24px; top: 38px; width: auto; white-space: nowrap; font-size: 10px; padding: 4px 6px; } }
  @media (prefers-reduced-motion: reduce) { .cat-head { transition: none; } .cat-button:hover .cat-head { transform: none; } }
</style>
