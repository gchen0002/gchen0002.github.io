<div class="paper-surprise" data-paper-surprise hidden>
  <button class="paper-launch" type="button" aria-label="Send a paper airplane" title="Send a paper airplane">
    <svg viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m3 13 25-9-9 25-5-11-11-5Zm11 5L28 4m-14 14-1 8 5-5" /></svg>
  </button>
  <span class="paper-note" data-paper-note role="status" aria-live="polite"></span>
</div>

<style>
  .paper-surprise { position: absolute; left: max(0px, calc(50% - 196px)); top: 50%; transform: translateY(-50%); width: 36px; height: 40px; }
  .paper-surprise[hidden] { display: none; }
  .paper-launch { display: grid; place-items: center; width: 100%; height: 100%; border: 0; border-radius: 10px; padding: 5px; background: transparent; color: #c5b4de; transition: color 160ms ease; }
  .paper-launch:hover { color: #fff1cd; }
  .paper-launch:disabled { opacity: .35; cursor: default; }
  .paper-launch svg { width: 26px; height: 26px; }
  .paper-note { position: absolute; bottom: calc(100% + 7px); left: 0; white-space: nowrap; font: 11px/1.3 'DM Sans Variable', sans-serif; color: #d9cbe9; }
  :global(.paper-flight) { position: fixed; inset: 0; overflow: hidden; color: #e2ccff; pointer-events: none; z-index: 30; }
  :global(.paper-aircraft) { position: absolute; left: 0; top: 0; width: 30px; height: 30px; will-change: transform; }
  :global(.paper-flight svg) { display: block; width: 100%; height: 100%; }
  @media (max-width: 760px) { .paper-surprise { width: 26px; } .paper-launch { padding: 0; } }
  @media (max-width: 360px) {
    :global(.portfolio .personal-footer) { padding-top: 42px; }
    .paper-surprise { left: calc(50% - 13px); top: -4px; height: 30px; transform: translateY(-100%); }
  }
</style>
